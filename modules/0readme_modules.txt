
Clone or update modules' repositories in this directory by using update 
scripts in
  ../00_initmodules/scripts
In Visual Studio solutions, modules should be automatically updated when the
IGLib base projects are built (use rebuild to make sure that modules are
up to date).

